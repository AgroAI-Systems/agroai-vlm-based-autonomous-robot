# MOD-03 (VLM) — Python bağımlılıkları
# Python 3.10 veya 3.11 önerilir.
#
# Kurulum:
#     py -3.11 -m venv .venv
#     .venv\Scripts\Activate.ps1     (Windows)
#     source .venv/bin/activate      (Pi 4 / Linux)
#     pip install -r requirements.txt

# VLM inference — transformers + vikhyatk/moondream2 (revision 2024-08-26)
# İlk kullanımda HuggingFace cache'ine ~3.85 GB indirir.
transformers>=4.40.0
torch>=2.0.0
torchvision>=0.15.0   # vikhyatk/moondream2 model kodunun bağımlısı
pillow>=10.0.0
einops>=0.7.0

# Test çerçevesi
pytest>=8.0.0
pytest-cov>=4.1.0

# Not: Daha yeni moondream revizyonları (2024-11+) pyvips ister, libvips DLL
# Windows'ta sıkıntılıdır. Bu yüzden 2024-08-26 revizyonunu pin'liyoruz.
# Pi 4 (Linux) için libvips kolayca: sudo apt install libvips42
