# MOD-03 Pi 4 gereksinimleri
# Kurulum: pip install -r requirements_pi.txt

# Sistem paketleri (pip degil, apt ile kur):
#   sudo apt install -y python3-picamera2 python3-opencv libvips42 build-essential

# VLM inference
transformers==4.44.2
torch
torchvision
pillow>=10.0.0
einops>=0.7.0

# YOLO (MOD-02 + entegrasyon testi icin)
ultralytics>=8.0.0

# Test
pytest>=8.0.0
